import React, { useState } from "react";

type userProp = {
  email: string;
  password: string;
};

const Login = ({ email, password }: userProp) => {
  return <h1> login</h1>;
};

export default Login;
